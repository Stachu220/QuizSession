<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="QuizBox.QuizCreatorPage"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:helpers="clr-namespace:QuizBox.Helpers"
             Title="QuizCreatorPage">

    <ContentPage.Behaviors>
        <toolkit:StatusBarBehavior
             StatusBarStyle="LightContent"
             StatusBarColor="Transparent"
        />
    </ContentPage.Behaviors>

    <Grid RowDefinitions="Auto,*,Auto" Loaded="onStackLayout_Loaded">
        <Image
            Source="background9.jpg"
            Aspect="AspectFill"
            IsVisible="True"
            Grid.RowSpan="3" />

        <VerticalStackLayout Grid.Row="0" HorizontalOptions="Start" Spacing="2">
            <VerticalStackLayout.Margin>
                <OnPlatform x:TypeArguments="Thickness">
                    <On Platform="Android" Value="25,65,25,0"/>
                    <On Platform="iOS" Value="25,113,25,0"/>
                    <On Platform="WinUI" Value="25,25,25,0" />
                </OnPlatform>
            </VerticalStackLayout.Margin>

            <Label Text="Quiz Creator" FontSize="28" FontAttributes="Bold" TextColor="White" FontFamily="Ubuntu-Bold" Margin="0,0,0,2"/>
            <Label Text="What you will create today?" FontSize="20" FontFamily="Ubuntu-Regular" TextColor="Gray"/>
        </VerticalStackLayout>

        <Border Grid.Row="1" VerticalOptions="Center" HorizontalOptions="Center" Background="{StaticResource RichBlack}" StrokeThickness="0" StrokeShape="RoundRectangle 25,25,25,25" Padding="25" Margin="25,0,25,0">
            <VerticalStackLayout Loaded="onStackLayout_Loaded">
                <Label Text="Quiz Title" FontAttributes="Bold" TextColor="White" FontFamily="Ubuntu-Bold" FontSize="20" Margin="0,0,0,5"/>
                <Border x:Name="TitleEntryBorder" StrokeShape="RoundRectangle 150,150,150,150" StrokeThickness="2" Stroke="{StaticResource PictonBlue}" Background="#002937" Padding="5" Margin="10,0,10,0">
                    <Entry x:Name="TitleEntry" Placeholder="Title" WidthRequest="250" TextColor="White" FontFamily="Ubuntu-Regular" FontSize="16" Margin="5,0,5,0" Focused="OnEntryFocused" Unfocused="OnEntryUnfocused"/>
                </Border>

                <Label Text="Quiz Description" FontAttributes="Bold" TextColor="White" Margin="0,25,0,5" FontFamily="Ubuntu-Bold" FontSize="20"/>
                <Border x:Name="DescriptionEntryBorder" StrokeShape="RoundRectangle 150,150,150,150" StrokeThickness="2" Stroke="{StaticResource PictonBlue}" Background="#002937" Padding="5" Margin="10,0,10,0">
                    <Entry x:Name="DescriptionEntry" Placeholder="Description" TextColor="White" WidthRequest="250" FontFamily="Ubuntu-Regular" FontSize="16" Margin="5,0,5,0" Focused="OnEntryFocused" Unfocused="OnEntryUnfocused">
                        </Entry>
                </Border>

                <Frame
                    x:Name="addQuestionsFrame"
                    Padding="12"
                    BackgroundColor="{StaticResource PictonBlue}"
                    BorderColor="Transparent"
                    Margin="10,35,10,10"
                    CornerRadius="12"
                    HorizontalOptions="FillAndExpand"
                    VerticalOptions="Center">
                    <Grid
                        HorizontalOptions="FillAndExpand"
                        VerticalOptions="Center">
                        <HorizontalStackLayout
                            Spacing="10"
                            VerticalOptions="Center"
                            HorizontalOptions="Center">
                            <Label
                                FontFamily="MaterialDesignIcons"
                                Text="{x:Static helpers:MaterialDesignIconsFonts.Plus}"
                                FontSize="24"
                                TextColor="White"
                                VerticalOptions="Center" />

                            <Label
                                Text="Create Quiz"
                                FontSize="18"
                                FontFamily="Ubuntu-Regular"
                                FontAttributes="Bold"
                                TextColor="White"
                                VerticalOptions="Center" />
                        </HorizontalStackLayout>
                    </Grid>

                    <Frame.GestureRecognizers>
                        <TapGestureRecognizer Tapped="onAddQuestionsClicked" />
                    </Frame.GestureRecognizers>
                </Frame>
            </VerticalStackLayout>
        </Border>

        <AbsoluteLayout Grid.Row="2" Margin="25">
            <Button
                x:Name="CancelButton"
                WidthRequest="50"
                HeightRequest="50"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                Clicked="CancelButton_Clicked"
                FontSize="30"
                Padding="0"
                FontFamily="MaterialDesignIcons" 
                TextColor="White"
                Background="#FF2400"
                BorderWidth="0"
                CornerRadius="12"
                MinimumHeightRequest="50"
                MinimumWidthRequest="50"
                Text="{x:Static helpers:MaterialDesignIconsFonts.Close}"
            >
            </Button>
        </AbsoluteLayout>
    </Grid>
</ContentPage>
